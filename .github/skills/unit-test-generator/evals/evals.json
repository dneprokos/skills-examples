{
  "skill_name": "unit-test-generator",
  "evals": [
    {
      "id": 1,
      "prompt": "Generate unit tests for ExcelColumnNameGenerator",
      "expected_output": "A complete test file with file-scoped namespace, [TestFixture], [SetUp] (if needed), and test methods covering GetExcelColumnName with valid indices, boundary values (0, 1, 26, 27, 702, 703), invalid inputs (negative), and edge cases. Should use FluentAssertions and NUnit [TestCase] for parameterized tests. File location should be in Metso.HSC.SimUI.Windows.Common.Tests/Utils/.",
      "files": [
        "Metso.HSC.SimUI.Windows.Common/Utilities/ExcelColumnNameGenerator.cs"
      ]
    },
    {
      "id": 2,
      "prompt": "Generate unit tests for RecentFilesService",
      "expected_output": "A complete test file with mocked dependencies (if any), testing AddRecentFileAsync, GetRecentFiles, file persistence, max file limit, and ordering. Should use NSubstitute for mocking, FluentAssertions for assertions, async Task test methods, and proper temp file cleanup in finally blocks. File location should be in Metso.HSC.SimUI.Windows.Common.Tests/Services/.",
      "files": ["Metso.HSC.SimUI.Windows.Common/Services/RecentFilesService.cs"]
    },
    {
      "id": 3,
      "prompt": "Generate unit tests",
      "expected_output": "A message asking the user to specify the class name they want to create unit tests for. Should NOT generate any test code.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "Generate unit tests for UserService",
      "expected_output": "A complete Java test file using JUnit 5 (@Test, @ParameterizedTest, @BeforeEach, @ExtendWith(MockitoExtension.class)), Mockito (@Mock, @InjectMocks), and AssertJ. Should include @Nested class grouping per method, happy path tests, null/invalid input tests, exception assertions using assertThatThrownBy, and async tests if the service has async methods. Test class named UserServiceTest in src/test/java/ mirroring the source package.",
      "files": ["src/main/java/com/example/services/UserService.java"]
    },
    {
      "id": 5,
      "prompt": "Write tests for the calculator.py module",
      "expected_output": "A complete Python test file using pytest with @pytest.mark.parametrize for parameterized cases, @pytest.fixture for setup, nested classes (class TestAdd, class TestDivide, etc.) grouping tests by function, pytest.raises for exception testing, and plain assert statements. Test file at tests/test_calculator.py. Should cover happy path, boundary values, zero division, and None/invalid input cases.",
      "files": ["src/calculator.py"]
    },
    {
      "id": 6,
      "prompt": "Create tests for the UserList component",
      "expected_output": "A complete TypeScript test file (.test.tsx) using Vitest and React Testing Library. Should include a renderComponent helper, screen.getByRole / screen.findByRole queries, userEvent for interactions, AllProviders wrapper if the component uses context/query, vi.fn() for callback props, and it.each for parameterized validation cases. Should cover initial render, empty state, loading state, populated state, user interactions, and error states. Test file at tests/components/UserList.test.tsx.",
      "files": ["src/components/UserList.tsx"]
    }
  ]
}
